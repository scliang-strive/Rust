mod guessing_game; // 猜数字游戏
mod data_type;        // 常见数据类型
mod function;        // 函数练习
mod branches;           // 分支循环流程


// #[allow(dead_code)] 要允许未使用的代码，在前面加上

fn main() {
    // guessing_game::guessing_game();
    
    // data_type::data_type();
    // data_type::array_idnex();

    // function::function();
    // let number = function::five(20);
    // println!("five value: {number}");

    // branches::branches(45);
    // branches::branch_by_else_if(99);
    // branches::branch_by_let_if();


    // branches::branch_by_loop();
    // branches::branch_by_loop_lable();
    // branches::branch_by_for();
    // branches::branch_by_while();
}

