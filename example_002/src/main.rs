mod scope;     // 作用域
mod reference; // 引用
mod struct_; // 结构体的定义和使用

fn main() {
    // scope::str_scope();
    // scope::str_copy();
    // scope::str_int_copy_diff();
    // scope::value_move();
    // reference::param_reference();
    struct_::print_area();
}
